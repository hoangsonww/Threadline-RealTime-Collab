apiVersion: v1
kind: Service
metadata:
  name: threadline-web
  labels:
    app.kubernetes.io/name: threadline-web
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: threadline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: threadline-web
  ports:
    - name: http
      port: 80
      targetPort: http
